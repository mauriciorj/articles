import React from 'react';
import styled from "styled-components";
import { Box } from "@material-ui/core";

//It's not being in use yet

export const Header = styled(Box)`
  background-color: ${props => props.background};
`;

